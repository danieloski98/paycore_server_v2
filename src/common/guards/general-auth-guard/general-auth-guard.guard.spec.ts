import { GeneralAuthGuardGuard } from './general-auth-guard.guard';

describe('GeneralAuthGuardGuard', () => {
  it('should be defined', () => {
    expect(new GeneralAuthGuardGuard()).toBeDefined();
  });
});
